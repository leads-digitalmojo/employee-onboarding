rules_version = '2';

// Same rationale as firestore.rules: all uploads and downloads go through
// server code (Admin SDK), which bypasses these rules. Deny all direct
// client access.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
